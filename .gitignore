__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.pytest_tmp/
.ruff_cache/
.idea/
traces/
*.log
screenshots/
tests/
# 真机核对脚本（`experiment/real_check/**`）跟 `tests/` 同一性质：只在本地跑，
# 不进版本库。⚠ 注意 `experiment/` **已经被跟踪过**（history 里在库），单独加这一行
# 不会让已入库的文件离开仓库——要真正脱开得再跑一次 `git rm --cached -r experiment/`。
experiment/
_to_delete/
.claude/
.env

# 运行产物与数据（不该进版本库）
tracelog/
screenshot/
experiment_results/
vision_dump/
vision_dump_smoke/
assets/
checkpoints/
# 0910 起记忆落盘在仓库根级 memory/（一条记录一个 uuid 文件）——运行期产物
# 不进 git；唯一例外是 knowledge_memory/（运营手写的共享源，随源进 git）。
memory/step_memory/
memory/object_memory/
memory/episode_memory/
memory/voided-*/
memory/*/vectors.jsonl
memory/*/index.json
.workbuddy/
docs/experiments/
log
*.egg-info/
