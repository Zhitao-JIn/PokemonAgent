---
⚠ 你**上一次的输出不合法**，这是第 $attempt 次尝试。

原因：$reason

上次你输出的是：
$raw

**别再输出同样的东西。** 照着上面的要求改，只输出一个 JSON 对象。

如果原因是"不在 action space 里"：**这一帧当前只有「可用按键」里列出的那几个键能按，
别的键——包括你原本打算走的方向——这一帧按不了。** 这通常是因为画面被对话框、
选择框之类的东西挡住了：你上面写的目标没有错，但它这一帧推不动，
先按可用键里的那个把挡着的东西清掉，目标还在栈上，等画面恢复了再继续做它。
