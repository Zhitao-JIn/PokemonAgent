## 战斗中的行动选择

战斗行动菜单中的 `FIGHT`、`PKMN`、`ITEM`、`RUN` 是不同分支，不能把方向键移动光标
和 `a` 确认混成一次野外移动。选择 `FIGHT` 后要重新读取招式列表；选择 `PKMN` 后
要确认切换对象；选择 `ITEM` 后要确认道具列表。每次分支切换都应以新画面为依据。

野生战斗可以尝试 `RUN`，但逃跑可能失败；出现失败文字仍是一次有意义的结果，不能
继续重复同一个动作而不重新观察。
