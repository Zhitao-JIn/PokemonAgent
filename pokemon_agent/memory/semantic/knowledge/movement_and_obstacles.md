## 移动、阻挡与路线判断

按键后如果地图位置和画面没有变化，通常表示撞到了墙、树、建筑或其他不可通行格，
不能把这次按键当成已经前进。连续撞同一方向不会自动解决阻挡，应重新观察并换方向。

需要到达某个地点时，优先根据当前画面的 walk_map 和可通行方向逐步移动；只有确认
中间不会改变场景时才使用 `args.times` 连按。接近门、NPC、楼梯或草丛边界时，
应恢复单步移动并重新观察。
